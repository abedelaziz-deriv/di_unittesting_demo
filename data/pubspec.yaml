name: data
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  injectable: ^2.1.2
  remote:
    path: ../remote

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  injectable_generator: ^2.1.6
  build_runner: ^2.4.5
  mockito: ^5.4.2
