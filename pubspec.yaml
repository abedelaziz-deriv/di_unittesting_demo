name: di_unittesting_demo
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3
  get_it: ^7.6.0
  injectable: ^2.1.2
  url_launcher: ^6.1.11
  data:
    path: ./data/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  injectable_generator: ^2.1.6
  build_runner: ^2.4.5
  bloc_test: ^9.1.3
  mockito: ^5.4.2
  flutter_lints: ^2.0.1

flutter:

  uses-material-design: true

  assets:
    - assets/placeholder.jpg
